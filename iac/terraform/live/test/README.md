# live/test (referencia)

Stack por entorno. Aquí se definen `backend` (estado remoto) y composición de módulos.

> Este repo no contiene archivos `.tf`. Ver esqueletos en `../../base-files.md` y `docs/02-terraform-approach.md`.
